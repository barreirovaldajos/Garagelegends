# Secrets — never commit these
firebase-service-account.json
migration.env
migration-reset-links.txt
migration-summary.json
node_modules/

# Claude Code local session data
.claude/

# Firebase build cache
.firebase/

# Migration scripts dependencies (not part of the game)
package.json
package-lock.json

# Supabase backup (abandoned)
js/auth.supabase.backup.js

# Firebase client configs (cargados via CDN o variables de entorno)
js/firebase-config.dev.js
