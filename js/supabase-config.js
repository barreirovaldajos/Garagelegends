// Optional Supabase config. Leave empty to keep local-only mode.
// Never commit real keys for private environments if you do not want them public.
'use strict';

window.GL_SUPABASE_CONFIG = {
  url: '',
  anonKey: '',
  requireEmailConfirmation: true,
  allowSignup: true
};
